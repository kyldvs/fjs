foo || bar && baz

this_is_a_long_identifier || this_is_another_one && this_one_is_too || one_more_short_one;

this_is_a_naked_chain || of_logical_expressions || we_should_wrap_this || in_parenthesis_when_it_breaks;

this_is_a_naked_chain || (these_are_some_ands && that_fit_on_one_line) || this_is_a_naked_chain_2;

this_is_a_naked_chain || (these_are_some_ands && and_there_are_so_many && of_them_that_they_will_also && need_to_be_broken_up && after_the_first_chain) || this_is_a_naked_chain_2;
